#include "main.h"

/**
 * _abs - Computes the absolute value
 * of an integer.
 *
 * @i: input number as an integer.
 *
 * Retur: absolute value
 */int _abs(int i)
{
	if (i >= 0)
	{
		return (1);
	}
	else
	{
		return (i * -1);
	}
}
